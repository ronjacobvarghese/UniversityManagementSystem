import 'package:flutter/material.dart';

class TestsModal extends StatelessWidget {
  static String routeName = "/TestsModal";

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Color.fromRGBO(255, 255, 255, 1),
    );
  }
}
